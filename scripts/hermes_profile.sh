# Hermes editable install profile helper
# Source this from the hermes user's shell profile to prefer the editable install
export PATH=/opt/hermes/.venv/bin:$PATH
# Usage: source scripts/hermes_profile.sh
