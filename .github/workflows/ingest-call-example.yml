name: Example caller for ingest workflows
on: [push]

jobs:
  call-ingest:
    runs-on: ubuntu-latest
    steps:
      - name: Call ingest-integration workflow
        uses: ./.github/workflows/ingest-integration.yml
        with:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
