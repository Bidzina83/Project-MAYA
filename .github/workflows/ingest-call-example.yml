name: Example caller for ingest workflows
on: [push]

permissions:
  contents: read

jobs:
  call-ingest:
    uses: Bidzina83/Project-MAYA/.github/workflows/ingest-integration.yml@main
    secrets:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
